# TVCors Proxy 环境变量配置示例
# 复制此文件为 .env.local 并根据需要修改

# 服务端口 (可选，默认3001)
PORT=8001

# 默认User-Agent (可选)
DEFAULT_USER_AGENT=AptvPlayer/1.4.10

# 启用详细日志 (可选，默认false)
VERBOSE_LOGGING=false

# 允许访问的域名列表 (可选，为空表示允许所有域名)
# 多个域名用逗号分隔，支持通配符 *
# 示例: https://example.com,https://*.example.com,http://localhost:*
ALLOWED_ORIGINS=

# 常见配置示例:
# 只允许特定域名访问
# ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
# 
# 允许本地开发环境
# ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
# 
# 允许子域名
# ALLOWED_ORIGINS=https://*.yourdomain.com
#
# 混合配置
# ALLOWED_ORIGINS=https://yourdomain.com,https://*.yourdomain.com,http://localhost:*

# 运行环境
NODE_ENV=production
